# AI 股票交易实验运行规则

本项目是一个为期 30 天的加密货币合约研究与 paper trading 实验，在 Binance 合约模拟盘（demo，虚拟资金）上进行。
项目由股票/ETF 实验分叉而来，保留其风险限制和记录方式；日程改为 24 小时制：按 Asia/Kuala_Lumpur 时间每四小时检查一次，每天六次，连续三十个自然日，并通过每日复盘把经验反馈到之后的决策中。
它不是实盘交易系统，也不能被描述为稳定盈利系统。

## 安全默认值

- 默认只能使用 paper trading。这里的 paper 指 `demo-fapi.binance.com` 上的 Binance 合约模拟盘；任何脚本都不得向其他主机提交订单。
- 除非用户在聊天中明确授权某一笔具体实盘订单，否则实盘交易保持关闭。
- 未经人工确认，绝不提交真实资金订单。
- 如果任何 readiness 检查缺失、为 false、过期或含义不清，禁止下单。
- 如果市场条件不清楚，优先观察，不交易。
- 风险控制优先于收益。

## 平台范围

- 目标平台：Binance 模拟盘 USDT 永续合约账户，仅限 `05-交易记录-data/current-state.json` 观察列表中的合约，可做多或做空，同时只持有一个仓位。
- 杠杆由 `04-运行状态-state/paper-config.json` 固定为 5 倍逐仓，引擎在每次开仓前设置。杠杆只影响仓位占用的保证金：单笔亏损仍由止损和仓位计算规则限制。
- 模拟盘使用虚拟资金。本地密钥文件中的 `BINANCE_ENV` 必须为 `demo`；本项目禁止使用正式环境密钥。
- 本地 paper ledger 仍是成交、现金和风险的记录；当 `demo_order_execution_enabled: true` 时，账本记录的是模拟盘的实际成交。
- Binance API key 统一保存在项目根目录的 `09-API密钥-仅本地/binance-api.env`，只读检查脚本只读取此文件。不要要求用户把密钥粘贴到聊天窗口；分享项目时使用 `09-API密钥-仅本地/binance-api.env.example`，不要分享已填写的凭证文件。

## 允许做的事

- 读取策略、日程、状态、日志和证据文件。
- 在本地已有凭证时，执行只读的账户、持仓、订单和市场数据检查。
- 生成观察列表、风险备注和 paper trade 记录。
- 仅在 paper trading 范围内，按照策略和风险限制自主决定买入、卖出、减仓、止损、清仓、继续持有或空仓。
- 只有 `readiness.json` 中 `autonomous_paper_execution_enabled: true` 时才允许自动纸面执行；执行只能通过 `06-程序脚本-scripts/paper_engine.py`，它只向合约模拟盘提交市价单并写入本地账本。
- 记录“不交易”的决定、证据和理由。

## 禁止做的事

- 没有明确人工确认，禁止提交实盘订单。
- 禁止交易现货、期权、币本位或交割合约，禁止全仓保证金，禁止使用配置的 5 倍逐仓以外的杠杆，禁止低流动性合约和观察列表之外的任何合约。任何情况下都不得进行真实资金的合约交易。
- 禁止只根据单条新闻或单个指标下单。
- 禁止满仓、重仓或使用全部资金。
- 禁止把凭证写入日志、证据文件、命令输出或聊天内容。
- Paper trading 自主决策不授权实盘交易、真实订单、转账、修改账户设置或开启交易权限。

## 每次启动必须先读取

每个定时任务开始前必须读取：

- `AGENTS.md`
- `AGENTS.zh-CN.md`
- `02-项目文档-docs/TRADING-STRATEGY.md`
- `02-项目文档-docs/TRADING-STRATEGY.zh-CN.md`
- `03-定时任务-routines/schedule.json`
- `03-定时任务-routines/schedule.zh-CN.json`
- `03-定时任务-routines/CONTINUITY.md`
- `03-定时任务-routines/CONTINUITY.zh-CN.md`
- `04-运行状态-state/readiness.json`
- `05-交易记录-data/current-state.json`
- `05-交易记录-data/journal/` 中最新记录，如果存在
- `05-交易记录-data/reviews/LESSONS.md`

## 每次结束必须更新

每个定时任务结束时必须更新：

- `05-交易记录-data/current-state.json`
- `05-交易记录-data/journal/` 中当天交易日志
- `05-交易记录-data/evidence/` 中的证据说明

每条日志必须写清：

- 本次做了什么
- 为什么这么做
- 有没有提出订单
- 有没有提交订单
- 有没有成交
- 当前持仓
- 当前现金
- 当前风险
- 下一次任务重点
- 是否需要人工确认

## 币安运行入口

- 当 `BINANCE_ENV=demo` 时，检查脚本读取合约模拟盘主机上的 USDT 永续 `/fapi/` 接口。`02-项目文档-docs/BINANCE-API.zh-CN.md` 仍描述原来的股票设置；与本文件或交易策略冲突时，以本文件和交易策略为准。
- `06-程序脚本-scripts/run_observation.py` 只读。唯一的 POST 路径是 `06-程序脚本-scripts/demo_orders.py`，它固定指向模拟盘主机，且 `BINANCE_ENV` 不是 `demo` 时拒绝运行。
- 纸面规则强制：同时一个仓位、单仓名义价值最多 50%、单笔计划风险 0.5%、单日最大亏损 2%、止损距开仓价不超过 10%、开仓决策距其计划检查时间不超过 45 分钟、至少两类证据、净风险收益比至少 1.5、最长持仓 24 小时。开仓成交后，引擎立即在模拟盘挂出只减仓的止损和止盈触发委托（按标记价格触发），使止损在两次检查之间也有效；如果保护委托被拒绝，该仓位会被立即平仓。禁止手动下达、撤销或修改这些委托。
- 最后一个计划日期的最后一次检查必须平掉所有仓位。

## 复盘与改进

- 每日复盘遵循 `03-定时任务-routines/REVIEW-PROMPT.zh-CN.md`：用 `06-程序脚本-scripts/review_stats.py` 生成统计，写出复盘，并更新 `05-交易记录-data/reviews/LESSONS.md`。
- 每次交易检查都要读取并应用 `LESSONS.md`。经验只能让决策更严格，绝不能放宽或覆盖任何规则或风险限制。
- 对规则、限制、杠杆、观察列表、日程或代码的修改只能写入 `05-交易记录-data/reviews/PROPOSALS.md` 作为建议，只有用户可以批准和实施。
- 中英文均保留。日程日期在认证与审阅通过前仅为预备安排，配置日不计入实验交易日。
