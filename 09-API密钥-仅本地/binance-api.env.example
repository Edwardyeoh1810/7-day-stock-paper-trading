# Copy to 09-API密钥-仅本地/binance-api.env locally. Never share the completed file.
BINANCE_API_KEY=
BINANCE_API_SECRET=
# Use a Binance DEMO key only (demo.binance.com). Never a production key.
BINANCE_ENV=demo
